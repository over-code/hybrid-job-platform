# QA (ручная проверка)

## 1) Запуск
- [x] Live Server открывает проект без пустого экрана.
- [x] В консоли нет критических ошибок.

## 2) Роутинг
- [x] Переходы по меню работают без перезагрузки.
- [x] Прямая вставка `/#/route` открывает правильную страницу.
- [x] Несуществующий маршрут показывает 404.

## 3) Аутентификация (localStorage)
- [x] Регистрация создаёт пользователя в `hjp:users`.
- [x] Вход устанавливает `hjp:session.currentUserId`.
- [x] Выход очищает сессию.
- [x] Приватные страницы недоступны без входа.

## 4) Вакансии
- [x] Список вакансий рендерится из `hjp:vacancies`.
- [x] Фильтры меняют список.
- [x] Детали вакансии открываются.

## 5) Проекты
- [x] Список проектов рендерится из `hjp:projects`.
- [x] Детали проекта открываются.

## 6) Профиль
- [x] Профиль показывает данные текущего пользователя.
- [x] Изменения сохраняются в `hjp:users`.
- [x] «Расширенные настройки» раскрываются/закрываются с анимацией.
- [x] Данные кандидата (headline/уровень/город/формат/занятость/опыт/зарплата/стек/ссылки) сохраняются в `hjp:users[].candidateProfile`.
- [x] После сохранения `candidateProfile` мини-сводка слева обновляется сразу (без перезагрузки страницы).
- [x] При открытии «Расширенных настроек» левая карточка не растягивается по высоте.

## 7) UI
- [x] Ок на 360px и 1280px.
- [x] Элементы управления доступны с клавиатуры (минимум: tab).
